<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fypproject">  <!-- 確保 package 與 applicationId 一致 -->

    <!-- 網路權限（Firestore 必須） -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 定位權限（你已經有 fine，建議再加 coarse 作為備用） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 背景定位權限（如果 App 會在背景追蹤） -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">

        <!-- Google Maps API key（你已經有，保留） -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCYaTqLULpmg78HIGU0Wj9ZJZCB7XbLWRw" />

        <!-- Firebase 相關 meta-data 不要手動加，這會由 google-services.json 自動生成 -->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:theme="@style/AppTheme.Launch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Google Play Services Location -->
        <service
            android:name="com.google.android.gms.location.FusedLocationProviderService"
            android:exported="false" />
    </application>
</manifest>